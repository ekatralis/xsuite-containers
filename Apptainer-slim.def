Bootstrap: docker
From: ghcr.io/ekatralis/xsuite-slim:{{ VERSION }}

%arguments
    VERSION=latest

%environment
    # Unset BASH_ENV for Apptainer, as micromamba is read-only and cannot write to it. 
    unset BASH_ENV
    export PYTHONNOUSERSITE=1
